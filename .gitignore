# Dependencies
node_modules/

# EdgeOne
.env
app/.edgeone/
app/.tef_dist/
**/.edgeone/*
**/.tef_dist/*

# Local KV / dev data (runtime, not source)
app/data/kv.json

# WorkBuddy private working logs (keep local, never publish)
.workbuddy/memory/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Secrets — never commit
*secret*
*token*.env
