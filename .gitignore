# Dependencies
node_modules/

# Build output (Vite / SaaS 前端构建产物，非源码)
app/dist/
app/web/dist/
**/.vite/

# EdgeOne
.env
app/.edgeone/
app/.tef_dist/
**/.edgeone/*
**/.tef_dist/*

# Local KV / dev data (runtime, not source)
app/data/kv.json

# WorkBuddy private working logs (keep local, never publish)
.workbuddy/memory/
app/.workbuddy/

# OS
.DS_Store
Thumbs.db

# Logs
*.log

# Secrets — never commit
*secret*
*token*.env
