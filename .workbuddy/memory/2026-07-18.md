# 2026-07-18 工作日志

## AI 黑客松 — 基于 WorkBuddy 的专业领域生产系统

- 活动：公司全员 AI 黑客松，命题「基于 WorkBuddy 搭建专业领域生产系统」，三方向（金融投资研究 / 内容创作与发布分析 / 营销获客）。开始 6/29，提交截止 7/26，评审 7 月底-8 月中上旬。
- 决策：选定**方向 B 内容创作与发布分析**，**个人参赛**。产品创意采用"创意2 数据驱动选题命中率 为主 + 吸收创意1 多平台适配发布"。
- 产品命名：**选题罗盘 TopicCompass** —— 用创作者历史发布数据+实时热点为候选选题打"预期命中分"，一稿多平台适配，发布数据回流形成自我强化闭环。
- 部署方案：**EdgeOne Makers**（EdgeOne Pages 改名，原生支持 Agent+全栈Web+Serverless，每人 50万 token/月内置模型）。
- **关键技术约束**：EdgeOne 是边缘 Serverless，**不能用**本地 node:sqlite/长驻进程；选题历史库改用 **Edge KV**，数据回流用 **Pages Functions**（/functions 目录即路由，兼容 Hono/Express/Koa，支持 py/go runtime）。部署流程：`npm i -g edgeone` → `edgeone pages init/link/dev` → push Git 自动构建。
- WorkBuddy 能力映射（可复用核心资产）：Expert 选题评分官 + Skills（选题雷达/多平台改写/命中率复盘）+ Automation（每日选题简报、每周命中率复盘周报）。
- 已产出：`docs/项目方案书_选题罗盘.md`（完整方案书：定位/痛点/命题契合/架构/能力映射/可验证指标/8天计划/提交清单/风险/答辩钩子）。
- 待办：初始化 EdgeOne Makers 项目跑通预览链接 → 设计 Edge KV 数据结构 → 建 WorkBuddy Expert/Skills → 前端 Dashboard → Automation → 命中率验证。

## D1 骨架进展（下午）

- 架构图：把用户提供的四层架构截图重画为矢量 `docs/架构图_选题罗盘.svg`（去掉截图里的悬浮气泡），并替换方案书第 3 节的 ASCII 图为该 SVG 引用 + 分层文字说明。
- EdgeOne 骨架已建：`app/` 目录 = `index.html`（选题罗盘落地页 + 调用 /api/hello 按钮，含静态兜底）、`functions/api/hello.js`（EdgeOne 风格 onRequest 返回 JSON）、`package.json`（"type":"module"，dev/deploy 脚本）、`dev-server.mjs`（本地模拟 EdgeOne 静态托管 + /functions 路由，仅本地用）。
- edgeone CLI 装在隔离 workspace：`/Users/yangguang/.workbuddy/binaries/node/workspace/node_modules/.bin/edgeone`。注意新命令是 `edgeone makers`（`pages` 为兼容别名）。`makers dev` / `deploy` 都需登录或 `-t <API Token>`（EDGEONE_PAGES_API_TOKEN），需用户去腾讯云 EdgeOne Pages 控制台 settings 生成。
- 本地验证通过：`node app/dev-server.mjs` → `GET /api/hello` 正常返回 JSON。踩坑：被 import 的 `.js` 是 ESM，app/package.json 必须加 `"type":"module"`，否则报 `Unexpected token 'export'`。
- 预览链接（临时，云沙箱静态托管，走前端静态兜底演示 API）：https://b3a176547d5a4a4b940f31feeee2f1c3.tc-nanjing.share.codebuddy.woa.com
- 用户确认：预览链接「两个都要」——先云沙箱看效果，同时用户去拿 EdgeOne API Token，之后再部署 EdgeOne 正式版拿 edgeone.app 官方链接（评审最终要的那个）。

## EdgeOne Makers 正式部署成功（傍晚）

- 登录：用户在中国站控制台生成 API Token，CLI 用 `edgeone login --site china --token <TOKEN> --local` 登录成功。账号：杨光 / Account ID 100014677573 / APPID 1302452537。
- 部署命令：`edgeone makers deploy ./ -n topic-compass -t <TOKEN> --json`（在 app/ 目录下）。首次会自动创建 project。
- Project ID: `makers-tnahni7sxdy0`，Deployment ID: `dpdejqhdahit`。
- **官方预览链接（评审用）**：https://topic-compass-e84pfo9p.edgeone.cool —— 带 `eo_token` 一次性签名，命令行 curl 会 302 跳鉴权，需浏览器访问。含中国大陆加速区域的预览链接有效期 3 小时，过期需在控制台点「预览」更新；官方会每周一/周四生成长期专用链接。
- 控制台：https://console.cloud.tencent.com/edgeone/pages/project/makers-tnahni7sxdy0/deployment/dpdejqhdahit
- 关键认知：EdgeOne CLI 命令是 `edgeone makers deploy`（不是 pages），`--json` 输出机读结果含 url/projectId/deploymentId；`--area` 默认 global（含大陆加速，预览链接 3h 时效）。
- 待办：填黑客松问卷提交预览链接（https://doc.weixin.qq.com/forms/AJEAIQdfAAoAVMAxAZlAFoCNNs69fqmPf）；在控制台「项目设置」加环境变量后需重新部署一次才生效；后续做 Edge KV + Functions 数据回流 + Dashboard。

## 自定义域名决策（傍晚续）

- 默认 edgeone.cool 预览链接带一次性 eo_token 签名，过期后直接访问根路径报 401（不是服务问题），不适合填问卷/答辩。
- 域名选型：用户有 yeranyang.com（**已 ICP 备案**）和 yeranyang.cn（GitHub Pages 个人站）和 yeranyang.site（未备案）。**决定用 yeranyang.com 的子域 `topic.yeranyang.com`**，因已备案可绑 global 区域（含大陆加速），大陆可访问、无时效限制。
- 项目保持 **global 区域**部署（deploy 时 -a global）。绑定自定义域名只能在控制台「域名管理」页操作，CLI 不支持绑域名。
- DNS 解析在**腾讯云 DNSPod**（同账号，控制台通常可一键加解析）。
- 绑定步骤：控制台项目详情 → 域名管理 → 添加自定义域名 → 输入 topic.yeranyang.com → 按弹窗加归属权验证记录 + CNAME → HTTPS 证书需手动配（不自动生成）。
- **域名已绑定成功**（用户已完成绑定 + 申请证书 + 配 HTTPS 301）。验证：DNS 已解析（CNAME → topic.yeranyang.com.pages.dnsoe4.com → 211.97.84.7）；**HTTP(80) 返回 200**（内容/边缘就绪）；**HTTPS(443) 证书还在签发中**——边缘暂回默认证书 `*.cdn.myqcloud.com`，SAN 不匹配，故 curl http_code=000/TLS 报错。证书下发后自动恢复正常，无需操作。待证书生效后复验 https://topic.yeranyang.com 和 /api/hello。

## 方案书新增「部署方案」章节（傍晚续）

- 用户要求（部署进行中）先把部署方案补进方案书，方便答辩讲。
- 在方案书新增独立 **第 4 节「部署方案（EdgeOne Makers 实录）」**：含 4.1 为何选 Makers / 4.2 项目结构（functions 路由约定）/ 4.3 CLI 部署流程 / 4.4 踩坑表（ESM type:module、预览链接 401/302 一次性签名、区域↔备案耦合）/ 4.5 自定义域名（区域备案对照表 + topic.yeranyang.com 绑定 5 步）/ 4.6 环境变量需重部署生效。
- 原第 4~9 节顺延为 5~10，子标题 4.1/4.2/4.3 改 5.1/5.2/5.3；第 2 节「可运行」措辞改为"已真实部署+自定义域名"；第 7 节执行计划加「状态」列（D1 标 ✅ 已完成）；第 8 节提交清单首项勾选。

## D2-3 数据层完成（晚间）

- **架构**：写了存储抽象层 `functions/_lib/store.js`（createStore/resolveBackend/K key 约定/slugify），同一套代码生产走 EdgeOne KV（env.TOPIC_KV），本地走文件 KV（dev-kv.mjs LocalKV，持久化 data/kv.json）。**EdgeOne KV key 只允许 [0-9a-zA-Z_]，不能用冒号**，故 key 用下划线（post_xxx / topic_xxx）。
- **分析引擎** `functions/_lib/analytics.js`（纯函数无 IO，可独立测试）：engagementScore(阅读×0.01+点赞+在看×2+评论×2+分享×3+涨粉×4)、titleFeatures、timeBucket、analyze(爆款阈值=70分位)、scoreCandidates(基线40分+关键词匹配+时段+标题特征，输出0-100分+level 强推/可做/谨慎/不建议+可解释理由)。
- **API**（EdgeOne 目录式路由 functions/api/xxx/index.js）：`GET/POST /api/posts`（数据回流录入）、`GET /api/stats`（命中率总览+选题排名）、`POST /api/score`（候选选题评分）。公共工具 `functions/_lib/http.js`（json/ok/fail/preflight/readBody + CORS）。
- **dev-server 升级**：支持目录式路由（xxx.js 或 xxx/index.js）、POST body、注入 env.TOPIC_KV=LocalKV。启动：`node app/dev-server.mjs` :8788。
- **种子数据** `data/seed.mjs` 31篇（AI/效率/职场向公众号，含平台差异 wechat/shipinhao/xiaohongshu，故意让 避坑/AI工具实操/数字标题/晚间 高命中，随笔/深夜/蹭热点 低命中）。导入脚本 `seed-import.mjs`（POST 灌入）。
- **本地验证全通过**：31篇导入成功；stats 学出规律（整体命中率32.3%，晚间62.5% vs 深夜0%，避坑100%/AI工具实操75%）；score 有区分度可解释（AI工具提示词85强推 vs 深夜随笔35不建议）。
- **首页升级**：index.html 从 Hello 自检 → 「选题命中分现场评分」交互区（输入候选选题→打分+理由）+「历史命中率总览」（命中率/最佳时段/高命中选题Top5 条形图）。红=高命中（符合国内涨红语义）。保留 /api/hello 做环境自检徽标。
- **已部署** deployment dp7mtt4gqg83（global）。⚠️ 线上 stats/score 要真跑必须在控制台绑定 KV：项目→数据存储→绑定命名空间→变量名填 **TOPIC_KV**→重新部署一次。KV 需先在 Makers 控制台「KV存储」申请开通（需审批）+创建命名空间。未绑 KV 时前端优雅降级提示"未就绪"，Hello 仍工作。
- 自定义域名 topic.yeranyang.com：HTTP(80) 通但边缘函数疑似强制 HTTPS，证书未就绪前 /api/* 走 HTTP 空返回；待证书生效后统一验证 https 首页+API。
- KV 申请填写建议（已给用户）：数据量填 1GB（实际<100MB，单条1-2KB）、峰值QPS填50/日常5、场景=数据驱动选题命中系统读多写少。用户已提交 KV 开通审批。

## D4-5 智能层完成（晚间续）— WorkBuddy 资产

- **Expert「选题评分官」topic-scorer** 已建成并注册：目录 `~/.workbuddy/plugins/marketplaces/my-experts/plugins/topic-scorer`（注意插件元数据目录是 `.codebuddy-plugin/`，注册表 `.codebuddy-plugin/marketplace.json`）。agent 型，categoryId=06-ContentCreative。人设=数据驱动选题策略师，评分 rubric 与 analytics.js 完全一致（基线40/关键词+30/时段±10/标题特征），且会调用线上 GET /api/stats + POST /api/score 做真联动（非空壳提示词）。头像 ImageGen 生成后用 `sips -Z 512` 压到 512px/444KB（原图1.5M超500KB限制）。校验+注册通过（session-id a6c642b1-513b-4316-aa99-efa645f52a6e）。
- **三个项目级 Skill**（放 `AI Hackathon/.workbuddy/skills/`，团队共享、WorkBuddy 自动检测，frontmatter 带 agent_created:true）：
  - `topic-radar` 选题雷达（输入环节）：产结构化候选选题清单 + 可直接喂 /api/score 的 JSON。
  - `multi-platform-rewrite` 多平台改写（输出环节）：一稿适配公众号/视频号/小红书/知乎，附可扩展平台规则库 templates/platform-rules.md。
  - `hit-rate-review` 命中率复盘（回流环节）：读 /api/stats 出复盘周报，也是每周 Automation 执行体。
  - 三者首尾相接构成闭环：雷达产候选→评分官选→改写产出→回流→复盘反哺。
- 方案书第 5 节 5.1/5.2 已从"规划"改为"✅ 已实现"，D4-5 状态标 ✅ 已完成。
- 待办：绑定 KV 后重部署 + 灌种子数据到线上；配 Automation（每日选题简报 + 每周复盘周报）；证书生效后验证 https 线上评分；录 Demo + 填问卷提交。

## D7 自动化层完成（7/19，KV 仍审批中做的第一个闭环）

- **执行体** `app/scripts/generate-reports.mjs`（纯本地，不依赖线上 KV/不需起 dev-server，稳定可跑）：`node scripts/generate-reports.mjs daily|weekly|all`。复用 analytics.js + 数据源（优先级：回流数据 data/posts.json > 种子 seed.mjs，KV 就绪后同步 posts.json 即无缝切换）。内置 CANDIDATE_POOL 8 个候选选题模拟选题雷达产出。
- **产出物** 落到 `app/reports/`：daily-brief-<日期>.md + latest-daily-brief.md（Top3 高分选题+拟标题+命中理由+其余候选表+行动建议）；weekly-review-<日期>.md + latest-weekly-review.md（总览/高置信关键词/时段规律/标题规律/下周减法建议）。
- **数据严谨性优化**：周报关键词从"命中率优先"改为**置信度排序**（命中率 × log2(样本量+1)），加 ★ 徽标，让 `AI工具 70% n=10 ★★★`、`避坑 100% n=3 ★★★` 排在 `副业 100% n=1 ★☆☆` 之前，经得起评审追问。只改脚本，不动已验证的 analyze()。
- **两个定时 Automation 已注册并 ACTIVE**（automation_update create）：`选题罗盘·每日选题简报`（id automation-1784431544614，RRULE FREQ=DAILY BYHOUR=8 BYMINUTE=30）；`选题罗盘·每周命中率复盘`（id automation-1784431554389，RRULE FREQ=WEEKLY BYDAY=MO BYHOUR=9）。cwds 均指向 app 目录，prompt 让 WorkBuddy 跑脚本后读 latest-*.md 生成一句话/晨会式摘要推送。
- 本地跑通验证：daily Top1=避坑复盘 93 分强推；weekly 命中率 32%、31 篇、高置信 Top 关键词 AI工具/避坑/提示词。
- 方案书 5.3 Automation 改为 ✅ 已实现（两任务表+置信排序说明+平滑升级说明）；D7 状态 🟡 进行中；提交清单 WorkBuddy 资产包已勾选（Expert+3Skill+2Automation）。
- 至此四层：应用层骨架✅ / 数据层✅（本地，线上待 KV）/ 智能层✅ / 自动化层✅。剩：KV 批下来点亮线上评分 + 证书生效验 https + 应用层 Dashboard 完善 + 录 Demo + 填问卷。

## D6 应用层 Dashboard 升级为答辩级驾驶舱（7/19 下午）

- **重写 app/index.html** 为答辩级数据驾驶舱，区块：顶部 4 张 KPI 卡（历史样本/整体命中率/最佳时段/已建规律维度3类）+ 左「现场评分交互」+ 右「时段命中热力横条」+「高命中关键词 chips（置信排序+★徽标）」+「历史选题命中率排名表（mini-bar）」+ 底部「数据驱动闭环流程条」（雷达→评分官→改写→回流→复盘，标 Skill/Expert/Functions/Automation）。红=高命中（国内涨红语义）。
- **关键设计：优雅降级**。页面内嵌 `SNAPSHOT`（本地已验证的 31 篇真实 stats 数据 JSON）。数据源优先请求 /api/stats 实时数据，失败（如 KV 未绑返回 503）自动回退 SNAPSHOT 渲染 + 右上角状态点灰/绿切换（"演示快照"/"实时数据"）。评分区同理：/api/score 优先，失败用内嵌浏览器版评分引擎 localScore（与 analytics.js scoreCandidates 同逻辑）兜底。→ **KV/证书未就绪也能完整演示，就绪后自动升级为实时**。
- 本地验证全通过（首页所有区块+3 API 联通）。已部署 deployment **dppg48ijnyh8**（global）。
- **🎉 自定义域名证书已签发生效**：`https://topic.yeranyang.com` HTTP 200、证书 subject=CN=topic.yeranyang.com（TrustAsia DV TLS RSA CA 2025 签发）、首页含品牌、/api/hello HTTPS 正常返回 JSON。→ **这就是评审要的干净、稳定、无时效正式链接**（可填问卷/答辩）。线上 /api/stats 返回 503（KV 未绑，符合预期，页面走快照兜底）。
- 方案书执行计划：D6 应用层标 ✅ 已完成（含优雅降级说明）；D1 更新为「证书已签发、HTTPS 正式可用、无时效」；D2-3 数据层标 🟡 进行中（本地全通，线上待 KV）。
