{
  "name": "zhiwei-topic-compass",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "description": "知微 ZhiWei —— 数据驱动的选题命中与多平台发布分析 SaaS（AI 黑客松 · WorkBuddy + EdgeOne Pages）",
  "workspaces": [
    "web"
  ],
  "scripts": {
    "dev": "node dev-server.mjs",
    "dev:web": "npm run dev --workspace web",
    "build": "npm install --workspace web && npm run build --workspace web && node scripts/copy-functions.mjs",
    "preview": "npm run preview --workspace web"
  },
  "devDependencies": {
    "marked": "^18.0.0"
  }
}
